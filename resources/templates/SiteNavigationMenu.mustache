<nav id="{{& html-id }}" class="mirage-dropdown-container navigation-entry-item" aria-labelledby="{{& html-id }}-label" {{& html-tooltip }} aria-haspopup="true" tabindex="0">
	<h3 id="{{& html-id }}-label" {{& html-user-language-attributes }} class="dropdown-header">
		{{ header-text }}
		{{& html-dropdown-indicator }}
	</h3>
	<div class="dropdown-list dropdown-hide">
		<ul aria-label="submenu">
			{{# array-links }}
				<li id="{{& html-id }}"{{# array-sub-links }} class="entry-with-sub-list" aria-haspopup="true"{{/ array-sub-links }}>
					{{& html-link }}
					{{# array-sub-links }}
						{{& html-extend-indicator }}
						<ul class="dropdown-list dropdown-sub-list dropdown-hide" aria-label="submenu">
							{{# array-links }}
								<li id="{{& html-id }}">
									{{& html-link }}
								</li>
							{{/ array-links }}
						</ul>
					{{/ array-sub-links }}
				</li>
			{{/ array-links }}
		</ul>
		<div class="dropdown-list-anchor"></div>
	</div>
</nav>
