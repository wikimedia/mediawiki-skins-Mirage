<nav id="{{ id }}" class="{{& class }}" aria-labelledby="{{ id }}-label" {{& html-tooltip }} {{& html-user-language-attributes }}>
	<h3 id="{{ id }}-label">
		{{ label }}
	</h3>
	<ul class="skin-mirage-horizontally-oriented-list skin-mirage-tabs" role="tablist">
		{{& html-items }}
	</ul>
</nav>
